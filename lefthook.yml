pre-commit:
    commands:
        typecheck:
            run: bun run typecheck
        lint:
            run: bun run lint
        format:
            run: bun run format

commit-msg:
    commands:
        commitlint:
            run: bun run commitlint --edit
